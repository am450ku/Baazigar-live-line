# BAAZIGAR LIVE

Runnable Vite build assembled from the supplied Stitch HTML screens.

## Run

```bash
npm install
npm run dev
```

Then open the URL Vite prints (usually http://localhost:5173).

The app is currently a frontend/static build; Supabase/API credentials are not included.
